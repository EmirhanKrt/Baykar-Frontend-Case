import Header from '../Header';
import './index.css';

const HeroSection = () => {
  return (
    <section className="hero-section">
      <Header />
    </section>
  );
};

export default HeroSection;
